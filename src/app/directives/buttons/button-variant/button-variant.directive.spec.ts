import { ButtonVariantDirective } from './button-variant.directive';

describe('ButtonVariantDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonVariantDirective();
    expect(directive).toBeTruthy();
  });
});
